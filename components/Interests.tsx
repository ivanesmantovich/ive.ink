import styles from './Interests.module.scss'

export default function Interests() {
    return (
        <div className={`${styles.inputWrapper}`}>
            <span className={`${styles.interests}`}></span>
        </div>
    )
}
